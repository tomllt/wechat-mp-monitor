#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

if [[ -f "${ROOT_DIR}/dist/src/cli.js" ]]; then
  exec node "${ROOT_DIR}/dist/src/cli.js" "$@"
fi

exec node --import tsx "${ROOT_DIR}/src/cli.ts" "$@"
